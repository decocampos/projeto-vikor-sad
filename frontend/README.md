# Frontend VIKOR SAD

Interface React + Vite do MVP VIKOR.

```powershell
npm install
npm.cmd run dev
```

Por padrao, a aplicacao tenta ler `http://localhost:8000/api/article-case`. Para mudar a URL da API, crie um arquivo `.env` em `frontend/`:

```env
VITE_API_BASE_URL=http://localhost:8000
```
